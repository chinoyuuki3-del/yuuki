build:
  system: docker

dockerfile_path: "./Dockerfile"
working_directory: "/"
build_context_directory: "/"

start_command: "python main.py"

health_check:
  type: http
  http_path: "/health"
  interval_in_seconds: 30
